{
  "name": "react-bpmn-example",
  "version": "0.0.0",
  "description": "An example using react-bpmn inside a React application",
  "private": true,
  "scripts": {
    "start": "sirv . start --dev"
  },
  "license": "MIT",
  "dependencies": {
    "bpmn-js": "^8.1.0",
    "react-bpmn": "file:.."
  },
  "devDependencies": {
    "sirv-cli": "^1.0.10"
  }
}
